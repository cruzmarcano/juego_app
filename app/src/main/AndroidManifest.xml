<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cruzmarcano.juego">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <protected-broadcast android:name="android.intent.action.MEDIA_MOUNTED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- activity Carga -->

        <activity
            android:name=".CargaActivity"
            android:screenOrientation="landscape">

        </activity>

        <!-- activity principal o pantalla de inicio -->

        <activity
            android:name=".PrincipalActivity"
            android:screenOrientation="landscape"></activity>

        <!-- activity memoria -->
        <activity
            android:name=".MemoriaActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- activity lengua -->

        <activity
            android:name=".LenguaActivity"
            android:screenOrientation="landscape" />

        <!-- activity lista -->

        <activity android:name=".ListaActivity" />

        <!-- activity sonido -->
        <activity android:name=".Propias.SonidoListActivity" />

        <activity android:name=".EjercicioAtencion"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>